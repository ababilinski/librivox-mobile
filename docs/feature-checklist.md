# Audio Player Prototype Feature Checklist

This table is meant for AI agents auditing or extending the app. Paths are relative to `audio-player-prototype`. The required fields are `Feature Name`, `Feature Description`, `Function Description`, and `Script Location`; pass/fail/test fields are included so each row can be used directly as a validation checklist.

| Feature Name | Feature Description | Function Description | Script Location | Pass Condition | Fail Condition | Suggested Test |
|---|---|---|---|---|---|---|
| Application dependency graph | Initializes repositories, catalog sources, playback settings, downloads, read-along, Cast routing, diagnostics, and network monitoring at app startup. | `AudiobookApplication.onCreate` wires the app graph; `scheduleStartupBackgroundWork` refreshes seeded books and search cache after launch. | `app/src/main/java/com/librivox/mobile/AudiobookApplication.kt` | App launches with all graph dependencies available and seeded catalog refresh work scheduled without crashing. | Any screen crashes because a repository or service dependency is uninitialized. | Run `./gradlew testDebugUnitTest`; manually launch app and navigate Home, Library, Discover, Settings. |
| Startup seeded library refresh | Refreshes built-in LibriVox records for seeded books and keeps Pride and Prejudice in the library. | `refreshSeededLibriVoxBooks` fetches known IDs, upserts catalog books, updates autocomplete, and adds Pride and Prejudice. | `app/src/main/java/com/librivox/mobile/AudiobookApplication.kt`; `app/src/main/java/com/librivox/mobile/model/AudioBook.kt` | Seeded records refresh without removing user local state or creating stale placeholder books. | Seeded books disappear, duplicate, lose local download state, or stale placeholder Pride appears. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.model.AudioBookChapterTest`. |
| Main activity shell | Hosts Compose UI, permission requests, incoming intents, media controller lifecycle, and hardware volume delegation. | `MainActivity.onCreate`, `handleIncomingIntent`, and lifecycle callbacks initialize the UI and reconnect player state. | `app/src/main/java/com/librivox/mobile/MainActivity.kt` | App shell starts, resumes, handles new intents, and releases controller resources cleanly. | Returning to app loses playback state, crashes, or ignores app links/debug open intents. | Install debug build, background and foreground app during playback, then open a debug book intent. |
| Top-level navigation | Provides Home, Library, Discover, Settings, Downloads, Book Detail, and Book Reader routes. | `AudiobookHost` owns `NavHostContent`, top destination selection, route transitions, and deep book navigation helpers. | `app/src/main/java/com/librivox/mobile/ui/navigation/AudiobookHost.kt`; `app/src/main/java/com/librivox/mobile/ui/navigation/Routes.kt` | Bottom navigation and nested routes open the correct screen without stale back-stack restoration. | Tapping a tab or source link opens an old detail route or wrong destination. | Navigate through every tab, open detail and reader, press back, and repeat from player links. |
| Shared book transitions | Animates book covers between home/library/discover/detail surfaces using source-aware shared elements. | `SharedTransitions` and `BookSharedTransitionKey` coordinate transition keys passed by screens. | `app/src/main/java/com/librivox/mobile/ui/navigation/SharedTransitions.kt`; `app/src/main/java/com/librivox/mobile/ui/navigation/AudiobookHost.kt` | Opening a book from shelves or rows shows a stable cover transition with no flicker or wrong cover. | Transition jumps, uses another book cover, or blocks navigation. | Manual visual check on Home, Library, Discover opening Book Detail with animation speeds changed. |
| Debug open-book intent | Lets agents open a catalog result by query or ID directly to detail or reader. | `BookOpenIntent` parses extras; `AudiobookHost` resolves book detail hydration and navigates to detail or reader. | `app/src/main/java/com/librivox/mobile/ui/navigation/BookOpenIntent.kt`; `app/src/main/java/com/librivox/mobile/ui/navigation/AudiobookHost.kt`; `README.md` | Intent opens requested book and destination with toast/log feedback. | Intent times out, ignores query/ID, or opens the wrong destination. | Run README debug intent with `destination detail` and `destination reader`. |
| Profile onboarding | Collects profile, source, language, download, and permission choices on first launch. | `ProfileOnboardingScreen` normalizes selections and saves profile/settings before entering the app. | `app/src/main/java/com/librivox/mobile/ui/onboarding/ProfileOnboardingScreen.kt`; `app/src/main/java/com/librivox/mobile/model/ProfileRepository.kt` | Onboarding permits valid source/language combinations, persists profile, and does not force unwanted permissions. | Source/language choices become invalid, onboarding loops, or profile is not saved. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.ui.onboarding.ProfileOnboardingScreenTest`; reset onboarding via README command. |
| Network status monitoring | Tracks validated internet and online/offline state for UI and download-only behavior. | `NetworkStatusMonitor` listens to `ConnectivityManager` callbacks and exposes `NetworkStatus`. | `app/src/main/java/com/librivox/mobile/network/NetworkStatusMonitor.kt` | Offline UI only appears when network is unavailable; playback/streaming continues when app is backgrounded if network exists. | App shows false Wi-Fi/offline toast or blocks streaming after focus changes. | Toggle device network, background app during streaming, verify no false disconnected toast. |
| Home dashboard | Displays continue listening, daily picks, source shelves, local highlights, and quick entry points. | `HomeScreen` composes shelves and source cards from library, daily featured cache, and player state. | `app/src/main/java/com/librivox/mobile/ui/home/HomeScreen.kt`; `app/src/main/java/com/librivox/mobile/ui/home/HomeLogic.kt` | Home shows relevant shelves, current progress, and opens selected books correctly. | Empty or stale shelves appear despite available data, or cards open wrong books. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.ui.home.HomeLogicTest`; manual Home navigation. |
| Daily featured picks | Selects stable daily recommendations by enabled source and local calendar day. | `DiscoverRepository.loadDailyFeatured` and `selectDailyFeaturedAudiobooks` fill `DailyFeaturedCacheState`. | `app/src/main/java/com/librivox/mobile/catalog/DiscoverRepository.kt`; `app/src/main/java/com/librivox/mobile/catalog/DailyFeaturedCacheState.kt` | Picks stay stable for a day, change on day/source change, and prefer Wolne ordering when appropriate. | Daily picks churn within a day or ignore source settings. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.catalog.DailyFeaturedCacheStateTest`. |
| Discover search and browse | Searches or browses selected catalogs with query, field, language, source, sort, paging, and loading states. | `DiscoverScreen` captures controls; `DiscoverRepository.loadDiscoverPage` handles cache, remote loads, and pagination. | `app/src/main/java/com/librivox/mobile/ui/discover/DiscoverScreen.kt`; `app/src/main/java/com/librivox/mobile/catalog/DiscoverRepository.kt` | Search results match selected filters, show loading/error states, and support load more. | Query/filter changes show stale rows, wrong language/source rows, or broken paging. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.ui.discover.DiscoverLocalSearchTest`; manual search across fields. |
| Discover detail hydration | Enriches visible or selected catalog stubs with detailed metadata without blocking selected-book priority. | `hydrateVisibleBookDetails` queues background hydration; `hydrateSelectedBookDetail` prioritizes current selection. | `app/src/main/java/com/librivox/mobile/catalog/DiscoverRepository.kt` | Selected detail loads immediately and stale visible hydration does not overwrite or delay it. | Detail opens with missing chapters while background hydration keeps running against stale rows. | Search, tap a result quickly, verify details/chapters load before old visible rows hydrate. |
| Discover cache state | Persists and restores discover results, sort, source, and language when cache settings allow it. | `DiscoverCacheState.matches` validates cache reuse; `CatalogAutocompleteRepository` stores states. | `app/src/main/java/com/librivox/mobile/catalog/DiscoverCacheState.kt`; `app/src/main/java/com/librivox/mobile/catalog/CatalogAutocompleteRepository.kt` | Cached discover state restores only for matching query/filter/sort/source. | Old results appear under a different source, language, or sort. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.catalog.DiscoverCacheStateTest`. |
| Catalog autocomplete cache | Stores offline metadata for faster search suggestions and broad local searches. | `CatalogAutocompleteRepository` persists cached books; `CatalogAutocompleteRefreshWorker` refreshes cache under settings limits. | `app/src/main/java/com/librivox/mobile/catalog/CatalogAutocompleteRepository.kt`; `app/src/main/java/com/librivox/mobile/catalog/CatalogAutocompleteRefreshWorker.kt` | Search cache respects enabled source, cache-size limit, and automatic caching toggle. | Cache grows beyond configured limit or returns disabled-source rows. | Enable and disable automatic caching in Settings, then inspect Discover behavior and worker logs. |
| Multi-source aggregation | Fans out selected catalog requests across LibriVox, Lit2Go, Wolne Lektury, and Gutendex. | `AggregateCatalogClient` runs selected calls concurrently and merges results through `BookMerger`. | `app/src/main/java/com/librivox/mobile/catalog/AggregateCatalogClient.kt` | Disabled sources are skipped; enabled sources merge into a coherent result list. | Source settings are ignored or one failing source breaks all results. | Run catalog unit tests and manually switch source selections in Settings. |
| Book merge and deduplication | Combines duplicate catalog records while preserving primary source order, metadata, chapters, and local state. | `BookMerger.merge` and model merge helpers normalize match keys and fill missing fields. | `app/src/main/java/com/librivox/mobile/catalog/BookMerger.kt`; `app/src/main/java/com/librivox/mobile/model/AudioBook.kt` | Duplicate books merge once, local downloads survive, and source URLs are preserved. | Duplicate rows appear or local library/download state is lost after refresh. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.catalog.BookMergerTest --tests com.librivox.mobile.model.AudioBookChapterTest`. |
| Catalog query normalization | Cleans markdown/person/chapter links and generates variants for title, author, reader, chapter, and all-fields search. | `sanitizeCatalogSearchQuery`, `catalogSearchQueryVariants`, and ranking helpers normalize queries. | `app/src/main/java/com/librivox/mobile/catalog/CatalogSearchQuery.kt`; `app/src/main/java/com/librivox/mobile/catalog/CatalogSearchRanking.kt` | Searches match expected titles and discard unrelated fallback rows. | Punctuation, article, or markdown source strings produce bad searches or noisy results. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.catalog.CatalogSearchQueryTest --tests com.librivox.mobile.catalog.CatalogSearchRankingTest`. |
| LibriVox catalog source | Searches and hydrates LibriVox records including title, author, reader, genre, language, archive cover, and chapters. | `LibriVoxCatalogSource` maps LibriVox API/archive responses into `AudioBook`. | `app/src/main/java/com/librivox/mobile/catalog/LibriVoxCatalogClient.kt` | LibriVox searches return playable chapters with cover and metadata fallbacks. | Results lack playable URLs, covers, readers, or have unrelated fallback records. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.catalog.LibriVoxCatalogSourceTest`. |
| Lit2Go catalog source | Scrapes Lit2Go letter indexes and detail pages into playable audiobook records. | `Lit2GoCatalogSource` parses book-icon anchors, detail headings, chapter MP3s, thumbnails, and metadata. | `app/src/main/java/com/librivox/mobile/catalog/Lit2GoCatalogSource.kt` | Lit2Go browse/search/detail records include correct title and audio URLs. | Brand headings become book titles, chapters are missing, or thumbnail fallback fails. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.catalog.Lit2GoCatalogSourceTest`. |
| Gutendex catalog source | Uses Gutendex and Project Gutenberg metadata to find playable audiobook records and enrich cross-source matches. | `GutendexCatalogSource` parses formats, creators, languages, pages, covers, and audio availability. | `app/src/main/java/com/librivox/mobile/catalog/GutendexCatalogSource.kt` | Playable Gutendex audio rows appear while text-only stale rows are filtered from local search. | Text-only records appear as playable books or audio records miss metadata. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.catalog.GutendexCatalogSourceTest`. |
| Wolne Lektury catalog source | Loads Polish audiobooks, literary metadata, translator/original language data, audio parts, and source assets. | `WolneLekturyCatalogSource` parses indexes, tags, collection children, MP3 media, durations, stopka filtering, and EPUB/DAISY assets. | `app/src/main/java/com/librivox/mobile/catalog/WolneLekturyCatalogSource.kt` | Wolne books expose playable chapters, Polish metadata, translated-work info, and read-along assets. | Stopka appears as playable content, translators vanish, or collection children do not map to chapters. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.catalog.WolneLekturyCatalogSourceTest`. |
| Catalog source and language settings | Lets users choose catalog sources and supported languages, including Polish constraints for Wolne Lektury. | `BookSourcePreference`, `CatalogSourcePreference`, and language normalization feed Settings and aggregate catalog calls. | `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt`; `app/src/main/java/com/librivox/mobile/ui/settings/SettingsScreen.kt` | Polish is available when Wolne is enabled and removed or forced appropriately when source selections change. | Invalid language/source combinations are saved or Discover calls disabled sources. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.playback.BookSourcePreferenceTest`. |
| Library list | Shows saved/local/catalog books with sort, filters, search, progress, download state, and open actions. | `LibraryScreen` builds visible rows from repository state, progress, network state, and filters. | `app/src/main/java/com/librivox/mobile/ui/library/LibraryScreen.kt` | Library rows show accurate status and search metadata; tapping opens correct detail. | Search misses author/chapter/reader metadata or wrong books are hidden. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.ui.library.LibrarySearchTest`; manual Library checks. |
| Downloads-only library mode | Restricts Library to downloaded books when offline or when user enables downloads-only mode. | `LibraryScreen` computes `downloadsOnlyActive`; `LibraryOfflineBanner` explains state and exits mode. | `app/src/main/java/com/librivox/mobile/ui/library/LibraryScreen.kt`; `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt` | Offline and downloads-only modes show only saved books with clear banner copy. | Non-downloaded books appear offline or user cannot exit downloads-only mode when online. | Toggle airplane mode and downloads-only mode, then verify Library contents. |
| Library book action sheet | Provides row actions for play, open details, read source, queue, download, delete downloads, remove, external links, and donate. | `LibraryBookActionSheet` gates actions from book state, source links, and download availability. | `app/src/main/java/com/librivox/mobile/ui/library/LibraryScreen.kt` | Available actions match book state and source; destructive actions update repository/downloads. | Missing actions, unavailable actions, or delete/remove does not update state. | Long-press/open action sheet for local, LibriVox, Lit2Go, Gutendex, and Wolne books. |
| Book detail screen | Displays title, cover, metadata, description, source links, chapters, play/download controls, and related actions. | `BookDetailScreen` composes detail header, actions, metadata, chapters, source links, and staged hydration. | `app/src/main/java/com/librivox/mobile/ui/detail/BookDetailScreen.kt` | Detail screen is readable, opens selected chapter/playback, and reflects hydrated metadata. | Header overlaps, metadata is stale, or actions target wrong chapter/book. | Open detail from multiple sources and screen sizes; run relevant detail unit tests. |
| Book detail cover backdrop | Optional appearance mode uses the cover art as a distorted/faded backdrop behind the detail header. | `BookDetailScreen` consumes `bookDetailUseCoverBackdrop`; `SettingsScreen.AppearanceSettingsScreen` toggles it. | `app/src/main/java/com/librivox/mobile/ui/detail/BookDetailScreen.kt`; `app/src/main/java/com/librivox/mobile/ui/settings/SettingsScreen.kt`; `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt` | Backdrop is disabled by default, can be enabled, reaches the title/status area, and fades into body color. | Toggle defaults on, header area remains plain, or body fade/readability is poor. | Toggle Appearance setting in light and dark mode and screenshot Book Detail. |
| Book detail artwork color scheme | Optional appearance mode tints detail page from artwork while default stays stable/off. | `bookDetailUseArtworkColorScheme` preference feeds detail color decisions. | `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt`; `app/src/main/java/com/librivox/mobile/ui/detail/BookDetailScreen.kt`; `app/src/main/java/com/librivox/mobile/ui/theme/AlbumArtColorExtractor.kt` | Default is off; enabling applies readable artwork-derived colors. | Default enables volatile art colors or light-mode text becomes unreadable. | Run Settings appearance manual check on several covers in light/dark. |
| Book detail translation metadata | Shows Wolne translated-work pill and translated-by credit under director metadata when applicable. | `WolneLekturyCatalogSource` maps original language and translator; detail metadata helpers render pills/credits. | `app/src/main/java/com/librivox/mobile/catalog/WolneLekturyCatalogSource.kt`; `app/src/main/java/com/librivox/mobile/ui/detail/BookDetailScreen.kt` | Translated Wolne books show translated pill and translator/original language when known; original Polish books do not. | Translation data appears as a separate banner, is missing, or appears for non-Wolne/untranslated books. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.ui.detail.BookDetailTranslationMetadataTest`. |
| Book detail chapter list | Renders chapters with Material-style containers, play/download/status/action controls, and selected chapter context. | Chapter section composables in `BookDetailScreen` render playable chapter rows and download state. | `app/src/main/java/com/librivox/mobile/ui/detail/BookDetailScreen.kt` | Chapter rows feel grouped, readable, and expose play/download/check states correctly. | Chapters look bare, status icons mismatch, or actions overflow/overlap. | Manual UI check on multi-chapter Wolne and LibriVox books in light/dark. |
| Source links and donation links | Shows external source links, donation links for supported remote sources, and avoids donate buttons for local sources. | `AudiobookLinks` builds web/app links; `SourceDonationLink` maps source donation URLs; UI renders source actions. | `app/src/main/java/com/librivox/mobile/model/AudiobookLinks.kt`; `app/src/main/java/com/librivox/mobile/model/SourceDonationLink.kt`; `app/src/main/java/com/librivox/mobile/ui/components/BookSourceInfoRow.kt` | Source and donation links open correct URLs and are hidden for local-only books. | Links target wrong source, lose chapter context, or donate appears for local books. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.model.AudiobookLinksTest --tests com.librivox.mobile.model.SourceDonationLinkTest`. |
| Sharing | Shares current book or chapter using app links or public source links based on user preference. | `ShareLinkMode`, `shareText`, and UI share actions build a stable share payload. | `app/src/main/java/com/librivox/mobile/model/AudiobookLinks.kt`; `app/src/main/java/com/librivox/mobile/ui/components/AudiobookShare.kt`; `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt` | Share sheet contains expected title and selected app/public link mode. | Shared text omits title, uses wrong URL, or ignores preference. | Run AudiobookLinks tests and manually share from detail/player/library. |
| Manual downloads | Downloads individual chapters or full books on explicit user action. | `DownloadManager.downloadChapter` and `downloadAll` enqueue chapter, cover, and read-along asset work. | `app/src/main/java/com/librivox/mobile/download/DownloadManager.kt`; `app/src/main/java/com/librivox/mobile/download/ChapterDownloadWorker.kt`; `app/src/main/java/com/librivox/mobile/download/BookAssetDownloadWorker.kt` | Explicit download queues missing files and updates UI states from queued to downloaded. | Nothing is enqueued, duplicate work floods, or state stays queued/downloading forever. | Download a chapter and a full Wolne book, then verify files and UI state. |
| Book-level source asset downloads | Downloads audio EPUB, DAISY ZIP, and EPUB reader assets for Wolne books when needed. | `downloadableReadAlongAssets`, `enqueueReadAlongAsset`, and `BookAssetDownloadWorker` manage book-level assets. | `app/src/main/java/com/librivox/mobile/model/AudioBook.kt`; `app/src/main/java/com/librivox/mobile/download/DownloadManager.kt`; `app/src/main/java/com/librivox/mobile/download/BookAssetDownloadWorker.kt` | Wolne full download saves audio/read-along assets and opens reader offline when possible. | Full download omits source text assets or inflates asset counts incorrectly. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.model.AudioBookChapterTest`; manually download Wolne book and open reader offline. |
| Cover downloads and local-first artwork | Downloads higher-quality covers and prefers local downloaded covers in library, detail, and player. | `BookCoverDownloadWorker`, `AudioBook.coverUri`, `systemArtworkUri`, and `BookCover` resolve local art before remote thumbnails. | `app/src/main/java/com/librivox/mobile/download/BookCoverDownloadWorker.kt`; `app/src/main/java/com/librivox/mobile/model/AudioBook.kt`; `app/src/main/java/com/librivox/mobile/ui/components/BookCover.kt` | Downloaded cover replaces stale thumbnail everywhere and survives catalog refresh. | Remote thumbnail still appears after redownload or local cover is lost on merge. | Run model tests covering downloaded cover filenames and merge preservation; manual redownload check. |
| Delete downloads and undo | Deletes chapter/book downloads, can stage delete for undo, and cleans book assets/covers. | `deleteDownload`, `deleteDownloads`, `stageDeleteDownloads`, `restoreStagedDownloads`, and `discardStagedDownloads` manage files and repository state. | `app/src/main/java/com/librivox/mobile/download/DownloadManager.kt` | Deleted files disappear and repository state clears; staged undo restores the same files. | Files remain on disk, state claims downloaded after delete, or undo restores wrong file. | Download, delete, undo if UI exposes it, and inspect Library state. |
| Downloads screen | Shows downloaded or downloadable books/chapters with progress, status, delete, and open actions. | `DownloadsScreen` renders grouped download state from library and invokes `DownloadManager`. | `app/src/main/java/com/librivox/mobile/ui/downloads/DownloadsScreen.kt` | Downloads page accurately shows queued/downloading/downloaded/partial books. | Page hides active downloads or delete/download buttons do not update state. | Open Downloads from Library/Settings, start downloads, delete, and verify rows. |
| Download network policy | Separates explicit download constraints from auto-download behavior and lets book-info assets use any connection. | `DownloadNetworkPolicy` and `AutoDownloadMode` define constraints; `PlaybackSettingsStore` persists them. | `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt`; `app/src/main/java/com/librivox/mobile/ui/settings/SettingsScreen.kt`; `app/src/main/java/com/librivox/mobile/download/DownloadManager.kt` | Wi-Fi only, cellular + Wi-Fi, and manual/off auto-download settings produce expected WorkManager constraints. | Manual disables explicit downloads or Wi-Fi-only blocks metadata/book-info downloads. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.playback.DownloadNetworkPolicyTest`. |
| Background playback service | Owns all Media3 playback, audio focus, queue setup, media notification, progress saving, downloads, and Cast transfer. | `AudiobookPlaybackService` creates local/remote players, handles session callbacks, custom commands, and service lifecycle. | `app/src/main/java/com/librivox/mobile/playback/AudiobookPlaybackService.kt` | Playback continues in background, notification controls work, and app reconnects to current state. | Playback stops unexpectedly, duplicate players compete, or controls desync from service. | Manual playback test with backgrounding, lock screen/notification controls, and app relaunch. |
| Play book/chapter queue setup | Starts playback from selected book/chapter and builds correct MediaItems with metadata. | Service session callbacks and `AudioBookChapter.toMediaItem` translate app books into Media3 items. | `app/src/main/java/com/librivox/mobile/playback/AudiobookPlaybackService.kt`; `app/src/main/java/com/librivox/mobile/model/AudioBook.kt` | Selected chapter starts at expected index and queue contains all playable chapters. | Wrong chapter starts, local/downloaded file is ignored, or queue has placeholder chapters. | Play from Library, Detail chapter, Reader, and Up Next. |
| Progress save and resume | Saves playback position per book/chapter and resumes from last meaningful position. | `ProgressStore` persists snapshots; service saves on periodic ticks and playback events; player state reads progress. | `app/src/main/java/com/librivox/mobile/model/ProgressStore.kt`; `app/src/main/java/com/librivox/mobile/playback/AudiobookPlaybackService.kt`; `app/src/main/java/com/librivox/mobile/playback/PlayerStateRepository.kt` | Returning to a book resumes near saved position and progress displays consistently. | Position resets to zero or jumps to wrong chapter after app restart. | Play, seek, stop app, relaunch, and resume same book. |
| Player state repository | Bridges MediaController, library metadata, queue, scrub previews, Cast state, bookmarks, and UI state. | `PlayerStateRepository` observes controller events and emits `PlayerState` for mini/full player and reader. | `app/src/main/java/com/librivox/mobile/playback/PlayerStateRepository.kt` | UI updates promptly for title, chapter, progress, cast, speed, queue, and scrub preview. | UI lags, shows stale artwork/title, or scrubbing affects wrong position. | Manual playback with chapter switches, scrub, Cast, and library cover refresh. |
| Mini player | Compact playback bar with artwork/title, play/pause, optional progress, optional Cast, and optional secondary controls. | `PlayerSheet` mini-player composables use `PlayerSheetState`, settings, and `miniPlayerHeight`. | `app/src/main/java/com/librivox/mobile/ui/player/PlayerSheet.kt`; `app/src/main/java/com/librivox/mobile/ui/player/PlayerSheetState.kt`; `app/src/test/java/com/librivox/mobile/ui/player/PlayerSheetLayoutTest.kt` | Mini player shrinks when progress bar is hidden and respects secondary/Cast/skip settings. | Hidden progress leaves old height or controls overlap/truncate. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.ui.player.PlayerSheetLayoutTest`; toggle mini-player settings. |
| Full Now Playing sheet | Expanded player shows artwork, title, credits, metadata, progress, transport controls, feedback, Cast, queue panels, and actions. | `PlayerSheet` expanded content and subpanel state manage Now Playing UI and panel navigation. | `app/src/main/java/com/librivox/mobile/ui/player/PlayerSheet.kt`; `app/src/main/java/com/librivox/mobile/ui/player/PlayerSubPanelState.kt` | Expanded player is readable, interactive, and synchronized with playback. | Controls overlap, metadata links navigate stale screens, or sheet gets stuck. | Expand/collapse player, use each action, rotate/change viewport if supported. |
| Cover art display modes | Supports different cover fitting modes including distorted/fill behavior for non-square covers. | `CoverArtDisplayMode`, `CoverArtDisplay`, and `BookCover` render artwork and local/remote fallbacks. | `app/src/main/java/com/librivox/mobile/playback/CoverArtDisplayMode.kt`; `app/src/main/java/com/librivox/mobile/ui/components/CoverArtDisplay.kt`; `app/src/main/java/com/librivox/mobile/ui/components/BookCover.kt` | Cover display mode changes are visible and do not crop important art unexpectedly. | Distortion/fill mode is ignored or cover renders blank. | Toggle Appearance cover display mode and inspect Now Playing/detail/library. |
| Transport controls | Provides play/pause, skip back, skip forward, previous/next chapter, scrub, and speed controls. | Service custom commands, `PlayerSheet`, reader controls, notification actions, and glasses controls dispatch player operations. | `app/src/main/java/com/librivox/mobile/playback/AudiobookPlaybackService.kt`; `app/src/main/java/com/librivox/mobile/ui/player/PlayerSheet.kt`; `app/src/main/java/com/librivox/mobile/ui/reader/BookReaderScreen.kt` | Each control changes playback by expected interval/chapter and updates UI. | Buttons do nothing, skip wrong interval, or scrub desyncs active highlight. | Manual control pass from detail, mini player, full player, reader, notification, and glasses. |
| Playback speed | Persists global/per-book speed and cycles notification speed presets. | `PlaybackSettingsStore.speedFor/saveSpeed`, `PlaybackSpeed`, and service speed commands control playback parameters. | `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt`; `app/src/main/java/com/librivox/mobile/model/PlaybackSpeed.kt`; `app/src/main/java/com/librivox/mobile/playback/AudiobookPlaybackService.kt` | Speed clamps to supported range, labels correctly, and persists for current book. | Speed resets unexpectedly, labels are malformed, or notification speed cycle skips presets. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.model.PlaybackSpeedTest`; manual speed changes. |
| Sleep timer | Pauses playback after a timer, fade out, or end-of-chapter target. | `SleepTimerController` exposes timer state; `PlaybackBus` and player/reader sheets surface controls. | `app/src/main/java/com/librivox/mobile/playback/SleepTimerController.kt`; `app/src/main/java/com/librivox/mobile/model/SleepTimerState.kt`; `app/src/main/java/com/librivox/mobile/ui/player/PlayerSheet.kt` | Timer counts down, fades/pauses at target, and can cancel or end at chapter boundary. | Timer keeps playing, pauses too early, or UI state remains running after cancel. | Manual test with short timer and end-of-chapter mode. |
| Auto-download while listening | Optionally downloads current chapter or current plus next few chapters when playback starts/progresses. | `AudiobookPlaybackService.autoDownloadCurrentChapter` and `autoDownloadChaptersFrom` use `AutoDownloadMode` and `DownloadManager`. | `app/src/main/java/com/librivox/mobile/playback/AudiobookPlaybackService.kt`; `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt` | Auto-download queues only allowed look-ahead chapters and respects network policy. | Auto-download ignores Off mode, downloads entire book, or queues duplicate work. | Set each auto-download mode, start playback, inspect Downloads and WorkManager logs. |
| Notification controls | Media notification can show configurable like and speed custom actions. | `NotificationControlsMode` and service command layout update custom session commands. | `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt`; `app/src/main/java/com/librivox/mobile/playback/AudiobookPlaybackService.kt`; `app/src/main/res/drawable/ic_notification_*.xml` | Notification actions match setting and update playback/feedback correctly. | Hidden actions still appear or action icons send wrong command. | Change Playback settings, play in background, inspect notification controls. |
| Feedback controls | Supports like/dislike feedback scoped to chapter, whole book, or hidden. | `PlaybackFeedbackStore` stores mutually exclusive feedback; UI and service read scope. | `app/src/main/java/com/librivox/mobile/playback/PlaybackFeedbackStore.kt`; `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt`; `app/src/main/java/com/librivox/mobile/ui/player/PlayerSheet.kt` | Feedback toggles are mutually exclusive and respect hidden/book/chapter scope. | Like and dislike both selected, chapter feedback leaks to book, or hidden controls remain visible. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.playback.PlaybackFeedbackStoreTest`. |
| Up Next queue panel | Shows queue, current item, reordering/removing/starting queue entries where supported. | `UpNextPanel` renders player queue and `QueueRepository` persists queued entries. | `app/src/main/java/com/librivox/mobile/ui/player/panels/UpNextPanel.kt`; `app/src/main/java/com/librivox/mobile/model/QueueRepository.kt`; `app/src/main/java/com/librivox/mobile/model/QueueEntry.kt` | Queue rows match playback queue and actions update queue without corrupting current item. | Queue displays stale entries or starts wrong book/chapter. | Add books to queue from Library/detail, open Up Next, start and remove entries. |
| Player chapters panel | Shows current book chapters with playback/download/feedback controls inside the player. | `ChaptersPanel` renders chapter list from `PlayerState` and delegates play/download/feedback actions. | `app/src/main/java/com/librivox/mobile/ui/player/panels/ChaptersPanel.kt`; `app/src/main/java/com/librivox/mobile/playback/PlayerStateRepository.kt` | Current chapter is highlighted and chapter actions target the right chapter. | Highlight is stale or chapter action affects another item. | Open Chapters panel during multi-chapter book playback and switch chapters. |
| Bookmarks | Lets users create, view, scrub-snap to, and delete playback or reader bookmarks. | `BookmarkRepository` persists bookmarks; `BookmarksPanel`, reader selection, and slider snapping consume them. | `app/src/main/java/com/librivox/mobile/model/BookmarkRepository.kt`; `app/src/main/java/com/librivox/mobile/ui/player/panels/BookmarksPanel.kt`; `app/src/main/java/com/librivox/mobile/ui/components/WavySlider.kt`; `app/src/main/java/com/librivox/mobile/ui/reader/ReaderLogic.kt` | Bookmarks save with correct text/time, appear in player/reader, and snapping chooses nearest valid target. | Bookmarks attach to wrong book/chapter or slider snaps outside threshold. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.ui.player.bookmarks.* --tests com.librivox.mobile.ui.reader.ReaderLogicTest`. |
| Related panel and metadata taps | Opens fresh Discover searches from author, reader, genre, source, or related metadata without restoring stale details. | `RelatedPanel`, `DetailsPanel`, `PlayerSheet.openDiscoverFilter`, and `AudiobookHost.openDiscoverFilter` prefill Discover and clear stale back stack. | `app/src/main/java/com/librivox/mobile/ui/player/panels/RelatedPanel.kt`; `app/src/main/java/com/librivox/mobile/ui/player/panels/DetailsPanel.kt`; `app/src/main/java/com/librivox/mobile/ui/player/PlayerSheet.kt`; `app/src/main/java/com/librivox/mobile/ui/navigation/AudiobookHost.kt` | Tapping metadata opens Discover results for that metadata and no old detail route is shown. | Old book detail reappears or Discover query/filter is wrong. | Tap author/reader from detail and player, verify Discover results and back behavior. |
| Wavy progress slider | Renders progress, buffer/bookmark markers, scrub previews, and bookmark snap activation. | `WavySlider` and marker helper functions drive visual and interaction behavior. | `app/src/main/java/com/librivox/mobile/ui/components/WavySlider.kt` | Slider scrubs smoothly, markers activate only near valid targets, and duration clamps correctly. | Marker activation is always on/off or scrub can exceed duration. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.ui.components.WavySliderMarkerTest`. |
| Read-along repository | Locates downloaded/source read-along assets and parses them into reader books. | `ReadAlongRepository` chooses local sync-capable assets and invokes `WolneAudioEpubParser`. | `app/src/main/java/com/librivox/mobile/readalong/ReadAlongRepository.kt`; `app/src/main/java/com/librivox/mobile/readalong/WolneAudioEpubParser.kt` | Reader opens from downloaded audio EPUB/DAISY/EPUB where available and reports unavailable otherwise. | Reader ignores downloaded sync asset or chooses plain EPUB when timed source exists. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.readalong.ReadAlongRepositoryTest`. |
| Wolne read-along parser | Parses audio EPUB, DAISY, and ordinary EPUB into chapters, timed segments, table of contents, source formatting, footnotes, and search text. | `WolneAudioEpubParser.parse`, `parseSmilSegments`, `mapToAppChapters`, and `parseStaticBook` build `ReadAlongBook`. | `app/src/main/java/com/librivox/mobile/readalong/WolneAudioEpubParser.kt`; `app/src/main/java/com/librivox/mobile/readalong/ReadAlongModels.kt` | Timed and static Wolne assets map to correct app chapters and retain useful formatting. | Chapters map out of order, timing is lost, or boilerplate appears as text. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.readalong.WolneAudioEpubParserTest`. |
| Repeated title trimming in reader chapters | Prevents repeated book-title headings from appearing at every chapter after the first. | `dropLeadingRepeatedBookTitleHeadings` removes leading title headings in timed and static parser paths. | `app/src/main/java/com/librivox/mobile/readalong/WolneAudioEpubParser.kt`; `app/src/test/java/com/librivox/mobile/readalong/WolneAudioEpubParserTest.kt` | Later chapters start with chapter heading/body instead of repeated book title, while first chapter can keep title. | Every Anielka chapter starts with `Anielka` or first chapter title is removed unexpectedly. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.readalong.WolneAudioEpubParserTest`. |
| Reader screen | Displays source text with title/header, playback controls, search, table of contents, scroll handle, page breaks, selection, and settings. | `BookReaderScreen` builds reader items, handles list state, and coordinates playback/read-along interactions. | `app/src/main/java/com/librivox/mobile/ui/reader/BookReaderScreen.kt` | Reader opens expected book/chapter, text is readable, controls work, and state survives scrolling. | Reader hangs loading, displays wrong book, or controls do not target playback. | Open reader from detail, library action sheet, and debug intent for a Wolne book. |
| Reader follow playback | Scrolls reader to active playback text unless user selection or visibility rules suppress it. | `ReaderLogic` decides initial and follow-scroll targets; `BookReaderScreen.scrollToReadAlongPosition` performs scroll. | `app/src/main/java/com/librivox/mobile/ui/reader/ReaderLogic.kt`; `app/src/main/java/com/librivox/mobile/ui/reader/BookReaderScreen.kt` | Reader follows active segment when enabled and stays still during text selection or when segment is comfortably visible. | Reader jumps constantly, ignores playback, or scrolls while text is selected. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.ui.reader.ReaderLogicTest`; manual playback in reader. |
| Reader highlight modes | Highlights current source paragraph or proportional sentence range based on setting. | `ReaderHighlightMode`, `activeReaderTextRangeForTextBlock`, and proportional split helpers choose highlight region. | `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt`; `app/src/main/java/com/librivox/mobile/ui/reader/ReaderLogic.kt`; `app/src/main/java/com/librivox/mobile/ui/reader/BookReaderScreen.kt` | Source segment and sentence modes visually differ and track playback time. | Highlight covers wrong paragraph/sentence or old preferences map incorrectly. | Run ReaderLogic tests covering highlight preferences and proportional ranges. |
| Reader search and find all | Searches read-along text accent-insensitively and opens a bottom-sheet list of all hits. | `ReadAlongBook.search`, `ReaderSearchBar`, `FindAllSheet`, and `jumpToHit` implement search workflow. | `app/src/main/java/com/librivox/mobile/readalong/ReadAlongModels.kt`; `app/src/main/java/com/librivox/mobile/ui/reader/BookReaderScreen.kt` | Search finds expected text, highlights context, and jumps to selected hit. | Accented search fails, hits jump to wrong chapter, or find-all sheet is empty. | Run parser search test and manual reader search for accented Polish text. |
| Reader text selection actions | Adds context menu actions for Read, Copy, Bookmark, and Cast/Play from selected text. | `BookReaderScreen` context-menu keys and selection tracking map selected text to segment/timestamp targets. | `app/src/main/java/com/librivox/mobile/ui/reader/BookReaderScreen.kt`; `app/src/main/java/com/librivox/mobile/ui/reader/ReaderLogic.kt` | Selecting text offers appropriate actions and targets nearest timed segment/bookmark text. | Actions missing, selected text wrong, or playback starts from unrelated position. | Manual long-select in reader; run ReaderLogic selected-text bookmark test. |
| Reader table of contents and page controls | Provides source TOC, chapter headers, page breaks, page labels, and draggable scroll progress. | `ReaderContentsSheet`, `ReaderTableOfContents`, page-count helpers, and scroll target helpers map TOC/page state to list positions. | `app/src/main/java/com/librivox/mobile/ui/reader/BookReaderScreen.kt`; `app/src/main/java/com/librivox/mobile/ui/reader/ReaderLogic.kt` | TOC entries and page handle jump to expected sections with accurate labels. | TOC opens wrong chapter or page handle jumps erratically. | Run ReaderLogic scroll/page tests and manually jump through a multi-chapter Wolne book. |
| Reader footnotes and source formatting | Preserves EPUB text styling, inline spans, first-line indentation, headings, verse, quotes, and tappable footnotes. | Parser records `ReadAlongTextStyle`, spans, footnotes; reader renders styled blocks and footnote sheets. | `app/src/main/java/com/librivox/mobile/readalong/WolneAudioEpubParser.kt`; `app/src/main/java/com/librivox/mobile/ui/reader/BookReaderScreen.kt` | Formatted static EPUB text keeps heading/paragraph style and footnotes open in sheet. | Text becomes flat/plain, first-line indent is wrong, or footnotes cannot open. | Run `parse_staticEpubKeepsInteractiveTocAndReaderFormatting` in Wolne parser tests; manual footnote tap. |
| Reader settings | Lets users change follow playback, highlight current text, highlight mode, text scale, speed, and sleep timer from reader. | `ReaderSettingsSheet`, `ReaderHighlightModeSheet`, `ReaderSpeedSheet`, and `ReaderSleepTimerSheet` persist or dispatch settings. | `app/src/main/java/com/librivox/mobile/ui/reader/BookReaderScreen.kt`; `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt` | Reader settings persist and immediately change reader/playback behavior. | Settings reset, exceed text scale bounds, or affect unrelated screens incorrectly. | Toggle every reader setting, leave/reopen reader, verify state. |
| Custom Cast route UI | Provides in-app Cast picker, route state, mini controller, transfer actions, and route volume controls. | `CastRouteRepository` bridges MediaRouter/Cast SDK to Compose; `CastSheets` renders custom route UI. | `app/src/main/java/com/librivox/mobile/ui/cast/CastRouteRepository.kt`; `app/src/main/java/com/librivox/mobile/ui/cast/CastSheets.kt`; `app/src/main/java/com/librivox/mobile/ui/components/AudiobookCastActions.kt` | Custom Cast sheet discovers devices, connects/disconnects, and shows route status/volume accurately. | Fake or stale devices appear, volume controls do nothing, or connection state desyncs. | Test on real Cast device with custom UI mode. |
| Google default Cast UI mode | Optionally uses Cast SDK route chooser and expanded controller instead of custom in-app Cast surface. | `CastSdkUi.showRouteChooserDialog`, `AudiobookExpandedControllerActivity`, and settings mode switch route UI. | `app/src/main/java/com/librivox/mobile/cast/sdk/CastSdkUi.kt`; `app/src/main/java/com/librivox/mobile/cast/sdk/AudiobookExpandedControllerActivity.kt`; `app/src/main/java/com/librivox/mobile/ui/settings/CastSettingsScreen.kt` | Google default mode opens official chooser/controller and respects restart-bound settings. | Custom UI still appears in Google mode or controller fails to open. | Switch Cast UI mode, restart if prompted, test route chooser on device. |
| Cast media metadata | Converts audiobook chapters into receiver-safe MediaItems with title, subtitle, artwork, duration, and castable URL selection. | `AudiobookCastMediaItemConverter` selects local HTTP or remote URLs and builds Cast metadata. | `app/src/main/java/com/librivox/mobile/playback/AudiobookCastMediaItemConverter.kt`; `app/src/main/java/com/librivox/mobile/playback/AudiobookPlaybackService.kt` | Cast queue shows meaningful book/chapter metadata and rejects non-receiver URLs. | Receiver sees local file URI, blank metadata, or wrong title/subtitle. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.playback.AudiobookCastMediaItemConverterTest`. |
| Local Cast HTTP server | Serves downloaded/local audio and cover art over receiver-reachable HTTP with range and CORS headers. | `LocalCastHttpServer` registers sources, serves GET/HEAD/range requests, and caches asset/file sources. | `app/src/main/java/com/librivox/mobile/playback/LocalCastHttpServer.kt` | Local/downloaded media can cast when local network is usable and range requests work. | Receiver cannot load local media, range requests fail, or server serves without local network access. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.playback.LocalCastHttpServerTest`; real Cast local file test. |
| Cast session transfer | Transfers playback between local ExoPlayer and remote CastPlayer while preserving queue, position, speed, and progress. | `AudiobookPlaybackService` Cast session callbacks swap active players and restore local playback on disconnect. | `app/src/main/java/com/librivox/mobile/playback/AudiobookPlaybackService.kt`; `app/src/main/java/com/librivox/mobile/playback/PlayerStateRepository.kt` | Starting/stopping Cast preserves current chapter/position and remote state is reflected in UI. | Transfer restarts book, loses queue, or local player continues behind remote player. | Cast during playback, seek remotely, disconnect, verify local resume position. |
| Cast hardware volume | Routes hardware volume keys to Cast route volume while Cast playback is active. | `MainActivity.dispatchKeyEvent` delegates to `CastRouteRepository.adjustVolumeBy` when custom Cast handling applies. | `app/src/main/java/com/librivox/mobile/MainActivity.kt`; `app/src/main/java/com/librivox/mobile/ui/cast/CastRouteRepository.kt` | Device volume buttons change receiver volume during Cast and normal volume otherwise. | Hardware keys adjust phone volume during Cast or no volume changes occur. | Validate on real Cast device with audio audible. |
| Cast settings | Exposes Cast UI mode, artwork colors, session resume, receiver stop, preload time, receiver app ID, local HTTP, verbose logs, diagnostics, reset, and advanced toggles. | `CastSettingsScreen` reads/writes `PlaybackSettingsStore` Cast preferences and labels restart-required choices. | `app/src/main/java/com/librivox/mobile/ui/settings/CastSettingsScreen.kt`; `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt` | Settings persist, restart-required settings are clear, and reset restores defaults. | Advanced choices do not persist or break CastOptionsProvider assumptions. | Toggle each Cast setting, restart app where required, verify value and behavior. |
| Cast diagnostics | Records Cast logs to memory/file and exposes diagnostics screen/overlay when enabled. | `CastDiagnosticsLogger` logs and rotates files; `CastDiagnosticsScreen` displays entries and export state. | `app/src/main/java/com/librivox/mobile/cast/diagnostics/CastDiagnosticsLogger.kt`; `app/src/main/java/com/librivox/mobile/ui/settings/CastDiagnosticsScreen.kt` | Diagnostics can be enabled, viewed, cleared, and logs survive enough to debug Cast issues. | Logs missing, grow unbounded, or diagnostics UI crashes. | Enable diagnostics, perform Cast actions, open diagnostics screen. |
| Projected glasses player | Provides projected AI glasses playback surface and input controls. | `GlassesPlayerActivity` connects `MediaController`, observes projected input/display, and renders compact Glimmer controls. | `app/src/main/java/com/librivox/mobile/glasses/GlassesPlayerActivity.kt` | Glasses UI reflects current playback and touchpad/button gestures control playback. | Projected UI cannot connect, gestures do nothing, or playback stops when display turns off. | Test with compatible projected device or emulator support, using tap/swipe/button gestures. |
| Playback bus | Shares sleep timer and speed state between service, phone UI, and glasses/reader surfaces. | `PlaybackBus` exposes `StateFlow` values updated by playback service and consumed by UI. | `app/src/main/java/com/librivox/mobile/playback/PlaybackBus.kt`; `app/src/main/java/com/librivox/mobile/playback/AudiobookPlaybackService.kt` | Speed and sleep timer changes appear consistently across player, reader, and glasses. | One surface shows stale sleep/speed state after another surface changes it. | Change speed/timer from player and reader, then verify glasses/phone UI. |
| Settings main screen | Organizes dense preferences into nested Appearance, Catalog, Downloads, Playback, Read Book, and Cast surfaces. | `SettingsScreen` and nested composables route settings areas and persist preferences. | `app/src/main/java/com/librivox/mobile/ui/settings/SettingsScreen.kt`; `app/src/main/java/com/librivox/mobile/ui/navigation/AudiobookHost.kt` | Settings are discoverable, grouped, and every toggle/radio updates persisted store. | Settings page becomes a flat cluttered surface or values do not persist. | Open every settings subpage, change values, restart app, verify persistence. |
| Appearance settings | Controls app theme, animation speed, artwork color scheme, cover backdrop, and cover art display mode. | `AppearanceSettingsScreen` writes theme/animation/artwork preferences; theme code consumes them. | `app/src/main/java/com/librivox/mobile/ui/settings/SettingsScreen.kt`; `app/src/main/java/com/librivox/mobile/ui/theme/AudioPlayerTheme.kt`; `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt` | Theme and visual toggles apply without unreadable text or one-note color failures. | Light/dark text contrast breaks, settings ignored, or defaults regress. | Toggle each appearance setting in light and dark; inspect Home, Detail, Player, Reader. |
| Playback settings | Controls audio content type, notification actions, mini-player behavior, feedback scope, and speed defaults. | `PlaybackSettingsScreen` writes `PlaybackSettingsStore` values consumed by service/player UI. | `app/src/main/java/com/librivox/mobile/ui/settings/SettingsScreen.kt`; `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt` | Settings update mini-player, notification, feedback, and audio attributes as expected. | Changing settings requires data clear or does not update active UI/service. | Toggle all playback settings during active playback and inspect UI/notification. |
| Download settings | Controls downloads-only mode, auto-download mode, and network policy. | `DownloadSettingsScreen` writes download preferences and links to Downloads page. | `app/src/main/java/com/librivox/mobile/ui/settings/SettingsScreen.kt`; `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt` | Download preferences persist and change WorkManager/download behavior. | Downloads-only or auto-download values are ignored. | Run download policy unit tests and manual Settings to Downloads workflow. |
| Catalog settings | Controls source selection, language selection, cache size, and automatic search caching. | `CatalogSettingsScreen` normalizes source/language choices and registers cache setting changes. | `app/src/main/java/com/librivox/mobile/ui/settings/SettingsScreen.kt`; `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt` | Catalog settings immediately affect Home/Discover source and language results. | Invalid language persists or disabled sources continue appearing. | Toggle source/language combinations and run BookSourcePreference tests. |
| Read Book settings | Controls default reader follow, highlight, highlight mode, and text scale. | `ReadBookSettingsScreen` edits `ReaderSettings` persisted in `PlaybackSettingsStore`. | `app/src/main/java/com/librivox/mobile/ui/settings/SettingsScreen.kt`; `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt` | Defaults apply when opening reader and are overrideable in-reader. | Reader ignores settings defaults or text scale exceeds clamps. | Change Read Book settings, open reader, verify defaults. |
| App theme | Applies Material theme, system/dark/light mode, tonal surfaces, and dynamic surface roles. | `AudioPlayerTheme`, `SheetThemeState`, and `TonalSurfaces` define app color surfaces. | `app/src/main/java/com/librivox/mobile/ui/theme/AudioPlayerTheme.kt`; `app/src/main/java/com/librivox/mobile/ui/theme/SheetThemeState.kt`; `app/src/main/java/com/librivox/mobile/ui/theme/TonalSurfaces.kt` | Screens have readable contrast and consistent Material surface hierarchy. | Text blends into background, chrome clashes, or player sheet colors desync. | Visual pass in system, light, and dark themes across main screens. |
| Library persistence | Stores library state, catalog upserts, local download state, seeded defaults, and de-duplication. | `LibraryRepository` persists `LibraryState` and exposes snapshot/update helpers. | `app/src/main/java/com/librivox/mobile/model/LibraryRepository.kt`; `app/src/main/java/com/librivox/mobile/model/AudioBook.kt` | Library state survives app restart and catalog refresh without losing local modifications. | Books duplicate, disappear, or lose downloaded/local fields after refresh. | Run model tests and manually restart after adding/removing/downloading. |
| Queue persistence | Stores user queue entries separately from active Media3 queue. | `QueueRepository` persists `QueueEntry` list for app-level queue workflows. | `app/src/main/java/com/librivox/mobile/model/QueueRepository.kt`; `app/src/main/java/com/librivox/mobile/model/QueueEntry.kt` | Queue survives restart and maintains intended order. | Queue duplicates, loses order, or references stale missing books. | Add several books to queue, restart app, verify Up Next. |
| Bookmark persistence | Stores per-book/chapter/time bookmarks, including selected reader text. | `BookmarkRepository` persists `Bookmark` records and exposes add/delete flows. | `app/src/main/java/com/librivox/mobile/model/BookmarkRepository.kt`; `app/src/main/java/com/librivox/mobile/model/Bookmark.kt` | Bookmarks survive restart and attach to correct book/chapter/time/text. | Bookmarks vanish, duplicate unexpectedly, or point to wrong chapter. | Create playback and selected-text bookmarks, restart, verify Bookmarks panel and reader. |
| Search history | Stores recent search history for Discover/autocomplete surfaces. | `SearchHistoryRepository` persists deduped `SearchHistoryEntry` values. | `app/src/main/java/com/librivox/mobile/model/SearchHistoryRepository.kt` | Recent searches appear, dedupe, and clear/update correctly. | Search history repeats every query or persists after clear. | Perform repeated searches and inspect history UI if surfaced. |
| Profile persistence | Stores user profile from onboarding for personalized app state. | `ProfileRepository` persists `Profile` JSON in DataStore. | `app/src/main/java/com/librivox/mobile/model/ProfileRepository.kt`; `app/src/main/java/com/librivox/mobile/model/Profile.kt` | Profile appears after onboarding and survives restart. | Profile resets or corrupts after app relaunch. | Complete onboarding, restart, verify onboarding does not repeat. |
| AudioBook model helpers | Derives local URIs, castability, numbered chapter titles, download counts, read-along assets, and local cover/download detection. | Extension/helper functions in `AudioBook.kt` centralize model behavior used by UI/download/playback. | `app/src/main/java/com/librivox/mobile/model/AudioBook.kt` | Helpers report correct local/downloaded/castable state across sources. | UI and service disagree about downloaded state or chapter display titles. | Run `./gradlew testDebugUnitTest --tests com.librivox.mobile.model.AudioBookChapterTest`. |
| Time and duration formatting | Formats durations, normalized durations, and timecodes for rows/player/reader. | `formatDuration`, `normalizedDurationMs`, and `TimecodeFormat` helpers produce display strings. | `app/src/main/java/com/librivox/mobile/model/PlaybackFormat.kt`; `app/src/main/java/com/librivox/mobile/ui/components/TimecodeFormat.kt` | Durations render consistently for short and long books/chapters. | Durations show negative, blank, or inconsistent strings. | Manual check player/detail rows for local, LibriVox, and Wolne durations. |
| HTML description rendering | Safely renders catalog descriptions with simple HTML formatting. | `HtmlText` converts HTML snippets into Compose text. | `app/src/main/java/com/librivox/mobile/ui/components/HtmlText.kt`; `app/src/main/java/com/librivox/mobile/ui/detail/BookDetailScreen.kt` | Description text is readable and does not expose raw tags. | HTML tags appear literally or formatting breaks layout. | Open details for sources with HTML descriptions. |
| Skeleton/loading states | Provides consistent loading placeholders for library, discover, and detail surfaces. | `SkeletonPlaceholders` and screen-level skeleton rows render loading state. | `app/src/main/java/com/librivox/mobile/ui/components/SkeletonPlaceholders.kt`; `app/src/main/java/com/librivox/mobile/ui/library/LibraryScreen.kt`; `app/src/main/java/com/librivox/mobile/ui/discover/DiscoverScreen.kt` | Loading states are stable and do not shift layout excessively. | Blank screens or janky layout jumps appear during loads. | Clear cache, open Discover/Home/Library on slow network, inspect placeholders. |
| Haptics and press feedback | Provides tactile/visual feedback for cards and controls without overriding system settings. | `BookCardPressFeedback` and `HapticTokens` centralize press/haptic behavior. | `app/src/main/java/com/librivox/mobile/ui/components/BookCardPressFeedback.kt`; `app/src/main/java/com/librivox/mobile/ui/components/HapticTokens.kt` | Controls provide appropriate feedback and respect device settings. | Feedback is absent, excessive, or ignores system haptic preference. | Manual tap/long-press pass on cards, sliders, player buttons. |
| Cast options provider | Provides Cast SDK options including receiver app ID and behavior derived from settings. | `AudiobookCastOptionsProvider` configures Cast framework at runtime startup. | `app/src/main/java/com/librivox/mobile/playback/AudiobookCastOptionsProvider.kt`; `app/src/main/java/com/librivox/mobile/playback/PlaybackAudioContentType.kt` | Cast SDK initializes with selected receiver app ID and restart-required changes behave predictably. | Receiver ID changes silently fail without restart warning or Cast SDK is misconfigured. | Change receiver app ID setting, restart app, verify Cast session startup. |
| Performance profiling script | Captures audio transition traces/logs for profiling playback transitions. | Shell script runs local profiling commands for audio transition investigation. | `scripts/profile-audio-transitions.sh`; `docs/audio-player-performance-investigation.md` | Script runs from repo root and produces useful capture output or clear errors. | Script references wrong package/activity or silently fails. | Run `scripts/profile-audio-transitions.sh` with a connected test device when profiling is needed. |
